{
  "description": "Docker alias role dependencies",
  "commands": [
    {
      "name": "docker",
      "message": "Install Docker using the recommended instructions for this host before using Docker aliases."
    }
  ]
}
