# Otherwise changes to some variables (such as $PROMPT) may be overwritten
setopt NO_GLOBAL_RCS

emulate sh -c "source $HOME/.path"
emulate sh -c "source $HOME/.exports"
emulate sh -c "source $HOME/.profile"
